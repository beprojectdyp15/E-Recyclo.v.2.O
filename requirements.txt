# ============================================================
# E-RECYCLO — Production Requirements (Vercel Optimized)
# ============================================================

# ── Core Django ──────────────────────────────────────────────
Django==4.2.7
asgiref==3.11.1
sqlparse==0.5.5

# ── Django Extensions ────────────────────────────────────────
djangorestframework==3.14.0
django-cors-headers==4.3.1
django-filter==23.5

# ── Database ─────────────────────────────────────────────────
psycopg2-binary==2.9.9
dj-database-url==2.1.0

# ── Static Files ─────────────────────────────────────────────
whitenoise==6.6.0

# ── WSGI Server ──────────────────────────────────────────────
gunicorn==21.2.0

# ── Environment Configuration ────────────────────────────────
python-decouple==3.8

# ── Image Processing ─────────────────────────────────────────
Pillow==10.1.0

# ── PDF Generation (used in vendor/collector/client reports) ─
reportlab==4.0.7

# ── Password Hashing (Argon2 — configured in settings) ───────
argon2-cffi==23.1.0
argon2-cffi-bindings==25.1.0

# ── HTTP Requests ────────────────────────────────────────────
requests==2.31.0
certifi==2026.1.4
charset-normalizer==3.4.2
idna==3.11
urllib3==2.6.3

# ── Security ─────────────────────────────────────────────────
cryptography==46.0.5

# ── Timezone Support ─────────────────────────────────────────
pytz==2025.2
tzdata==2025.3

# ── Utilities ────────────────────────────────────────────────
typing_extensions==4.15.0
packaging==26.0
six==1.17.0

# ── AI / ML — YOLOv8 E-Waste Detection ───────────────────────
# NOTE: These packages (~2–3 GB) will EXCEED Vercel's 500 MB limit.
# The app is fully functional without them — predictions will simply
# return a graceful error. Comment out these 4 lines for Vercel and
# host the AI predictor as a separate service (e.g. Railway/Render).
numpy==1.26.4
torch==2.10.0
torchvision==0.25.0
ultralytics==8.0.196
opencv-python-headless==4.8.1.78
